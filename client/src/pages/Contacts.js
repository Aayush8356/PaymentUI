import { VStack } from "@chakra-ui/react";
import React from "react";

const Contacts = () => {
  return (
    <div>
      <VStack maxW={["full", "container.xl"]} h={"100vh"} p={"16"}></VStack>
    </div>
  );
};

export default Contacts;
